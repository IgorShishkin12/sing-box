# Resolved config copies generated by runner-hw.sh from the *-ble.json templates
# (peripheral IDs substituted in via envsubst). Regenerated on every run.
configs/*-resolved.json

# Cached native/Android test binaries built on first run.
android-bins/
android-bins-*/

# Per-run log directories.
logs/

# Native test binary built by `runner-hw.sh mixed`.
loadtest/e2e-loadtest
