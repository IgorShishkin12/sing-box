name: retv2e2e-tcp

services:
  e2e-server:
    build:
      context: ..
      dockerfile: e2e/Dockerfile.server
    image: retv2e2e-server
    command: >
      -c "
        sum-server &
        sleep 2 &&
        sing-box run -c /etc/sing-box/config.json
      "
    volumes:
      - ./configs/server.json:/etc/sing-box/config.json:ro
    networks:
      - e2e-net
    healthcheck:
      test: ["CMD-SHELL", "pgrep -x sing-box > /dev/null 2>&1 || exit 1"]
      interval: 5s
      timeout: 5s
      retries: 18
      start_period: 30s

  e2e-client:
    build:
      context: ..
      dockerfile: e2e/Dockerfile.client
    image: retv2e2e-client
    command: >
      -c "
        sing-box run -c /etc/sing-box/config.json &
        e2e-loadtest --socks 127.0.0.1:1080 --url http://127.0.0.1:8080 &&
        echo 'ALL E2E TESTS PASSED'
      "
    volumes:
      - ./configs/client.json:/etc/sing-box/config.json:ro
    networks:
      - e2e-net
    depends_on:
      e2e-server:
        condition: service_healthy

networks:
  e2e-net:
    driver: bridge
